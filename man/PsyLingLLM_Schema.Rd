% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schema.R
\name{PsyLingLLM_Schema}
\alias{PsyLingLLM_Schema}
\title{PsyLingLLM Schema (v1)}
\description{
Definition of the standardized experiment output schema.
}
\details{
PsyLingLLM Schema (v1)

This defines the standardized output format for experiments in \pkg{PsyLingLLM},
including \code{trial_experiment()}, \code{conversation_experiment()},
and \code{factorial_trial_experiment()}.
}
\section{Core Fields}{

\describe{
  \item{Run}{Integer. Global sequential run index across all trials.}
  \item{Item}{Integer. Original item/material index.}
  \item{TrialPrompt}{Character. Prompt shown to the model.}
  \item{Material}{Character. Experimental stimulus or content.}
  \item{Response}{Character. Model's final output.}
  \item{Think}{Character or NA. Model reasoning trace (if available).}
  \item{ModelName}{Character. Model identifier (e.g., "deepseek-chat").}
}
}

\section{Timing Metrics}{

\describe{
  \item{TotalResponseTime}{Numeric (seconds). Total time from request to completion.}
  \item{FirstTokenLatency}{Numeric (seconds). Delay until the first token arrives
  (only present if \code{streaming = TRUE}).}
}
}

\section{Token Metrics}{

\describe{
  \item{PromptTokens}{Integer. Number of input tokens (NA if API does not provide).}
  \item{CompletionTokens}{Integer. Number of output tokens (NA if API does not provide).}
}
}

\section{Diagnostics}{

\describe{
  \item{TrialStatus}{Character. Either "SUCCESS" or "ERROR".}
  \item{Streaming}{Logical. Whether SSE streaming was used.}
  \item{Timestamp}{Character. Completion time of the run (ISO 8601).}
  \item{RequestID}{Character or NA. Provider request identifier if returned.}
  \item{RequestMessages}{Character (JSON). Messages sent to API for this trial,
  including system + prior turns + current user. Distinct from ConversationHistory,
  which also includes the model’s reply.}
  \item{ConversationHistory}{Character (JSON). Full conversation history
  including assistant replies after this trial.}
}
}

