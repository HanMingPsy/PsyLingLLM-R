% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/register_read.R
\name{infer_structure_from_placeholder}
\alias{infer_structure_from_placeholder}
\title{Infer structure from where \code{"\${CONTENT}"} lives}
\usage{
infer_structure_from_placeholder(body, placeholder = "${CONTENT}")
}
\arguments{
\item{body}{A list or character vector representing the raw input structure
in which the placeholder is to be searched.}

\item{placeholder}{Character(1). The placeholder string to search for,
typically \code{"\${CONTENT}"}.}
}
\value{
list(style, container_key, message_index, content_key, role_key, default_system)
}
\description{
- If \code{"\${CONTENT}"} sits inside a small list which is inside a larger list,
  treat it as messages-style.
  * content_key = name of the element holding \code{"\${CONTENT}"}
  * role_key    = the *immediately previous* element name in that small list
  * container_key = the named key that holds the sequence of messages
  * message_index = the numeric position in that sequence
- If \code{"\${CONTENT}"} is directly under a top-level key, treat as single-prompt.
}
