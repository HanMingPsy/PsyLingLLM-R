% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/error_handling.R
\name{validate_experiment_config}
\alias{validate_experiment_config}
\title{Validate experiment configuration and resolve API URL}
\usage{
validate_experiment_config(
  api_key,
  api_url,
  model,
  data,
  required_cols = c("Material"),
  registry = NULL,
  generation_interface = "chat"
)
}
\arguments{
\item{api_key}{Character(1). Provider API key.}

\item{api_url}{Character(1) or NULL. Optional for official providers.}

\item{model}{Character(1). Registry key (e.g., "deepseek-chat" or "deepseek-chat@proxy").}

\item{data}{data.frame/tibble. Must contain columns in \code{required_cols}.}

\item{required_cols}{Character vector. Columns that must be present in \code{data}.}

\item{registry}{Ignored (kept for backward compatibility).}

\item{generation_interface}{Character(1). Interface to validate, default "chat".}
}
\value{
Invisibly returns a list with resolved fields:
  \code{list(api_key, api_url, model_key, provider, generation_interface)}.
}
\description{
Applies PsyLingLLM's registry-first rules:
- Official providers may omit \code{api_url} if the registry defines
  \code{input.default_url}.
- Non-official providers (model key contains \code{"@"} or provider != "official")
  must supply \code{api_url}.
}
\details{
Also checks required data columns and API key presence.
}
